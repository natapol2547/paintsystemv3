"""Undo support for edits Blender's own undo does not cover.

`pixels` puts scripted pixel writes into Blender's image undo. The package
holds no classes and is not a registered submodule; import from it directly.
"""
